'use client'
import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: "#ff4545",
        },
        secondary: {
            main: "#ffffff"
        }
    }

})
export default theme;